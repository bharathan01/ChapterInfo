name: CI/CD to EC2

on:
  push:
    branches:
      - release

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v2

      # Step 2: Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: npm install

      # Step 4: Copy build files and deploy
      - name: Deploy to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: "44.203.169.14"
          username: ubuntu
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          source: "dist/*"
          target: "/home/ubuntu/chapter-info/dist"

      # Step 5: Run build on the server (after copying the files)
      - name: Run npm run build on EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: "44.203.169.14"
          username: ubuntu
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/chapter-info
            npm run build

      # Step 6: Restart app on EC2 via SSH
      - name: Restart app on EC2 via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: "44.203.169.14"
          username: ubuntu
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/chapter-info
            pm2 restart chapter-info || pm2 serve dist 3000 --name chapter-info
